using Game.HeaderPanel.View;

namespace Game.HeaderPanel.Model
{
    public class HeaderModel
    {
        private HeaderView _headerView;

        
        public HeaderModel(HeaderView headerView)
        {
            _headerView = headerView;
        }
    }
}
